# milo-website
Inbound email capture and outbound sending for Getmilo outreach.

## Endpoints
- `POST /api/send` — Send outbound email via Resend
- `POST /api/inbound` — Receive inbound email webhooks
- `GET /api/send` — Health check with env status
- `GET /api/inbound` — Health check
